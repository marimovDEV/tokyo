{
  "buildCommand": "pnpm install --ignore-scripts && pnpm run build",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "regions": ["iad1"],
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}

